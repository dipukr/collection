[
    {
    "id": 46,
    "title": "Synchronization",
    "content": "Synchronization is a mechanism which ensures that two or more concurrent processes or threads do not simultaneously execute some particular program segment known as critical section.Processes' access to critical section is controlled by using synchronization techniques. When one thread starts executing the critical section (serialized segment of the program) the other thread should wait until the first thread finishes. There is no any guarantee about exactly when a blocked thread is unblocked when the synchronized block is free again. This is typically up to the OS or execution platform to coordinate the unblocking of blocked threads. Thread synchronization is a mechanism which ensures that two or more concurrent threads do not execute some particular section of program especially critical section. There are two types of thread synchronization mutual exclusion and inter-thread communication.<div><div><div><br></div><div>Following are some classic problems of synchronization:<div><div><ul><li>Producer-consumer problem</li><li>Readers-writers problem</li><li>Dining philosopher problem</li></ul><div>Thread cooperation is achieved using the <span style=\"font-style: italic;\">wait-set</span> which is associated with the monitor and <span style=\"font-style: italic;\">wait-notify</span> mechanism.</div><div>Cooperation is important when one thread needs some data to be in a particular state and another thread is responsible for getting the data into that state.</div></div></div><div><br></div><div>When a thread invokes the <span style=\"font-style: italic;\">wait</span> method respect to an object then the thread will be suspended and added to the <span style=\"font-style: italic;\">wait-set</span> to wait until some other thread invokes <span style=\"font-style: italic;\">notify</span> or <span style=\"font-style: italic;\">notifyAll</span> on the same object. If a thread was put in the queue by a call to <span style=\"font-style: italic;\">wait</span> then it must be unfrozen by a call to <span style=\"font-style: italic;\">notify</span> before it can be scheduled for execution again.</div><div><span style=\"font-style: italic;\">Notify</span> method is used for waking up threads that are in the <span style=\"font-style: italic;\">wait-set</span> of the monitor of a particular object. </div><div>A thread may call <span style=\"font-style: italic;\">wait</span>, <span style=\"font-style: italic;\">notify</span> or <span style=\"font-style: italic;\">notifyAll</span> on an object only if it owns the monitor lock of that object.</div></div></div></div>"
  }
]